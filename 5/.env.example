AI_MODE=mock                    # mock / mock_dynamic / real
OPENAI_API_KEY=your_openai_api_key_here  # 仅在 AI_MODE=real 时需要
CACHE_TTL_SECONDS=600          # 缓存过期时间（秒），默认10分钟
RATE_LIMIT_PER_HOUR=50           # 每小时请求限制
